import React from "react";

const SectionTitle = ({ heading }) => {
  return (
    <div>
      <h2 className="text-3xl py-5 text-center font-bold">{heading}</h2>

    
    </div>
  );
};

export default SectionTitle;
